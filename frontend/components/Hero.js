import React from "react";

const Hero = () => {
  return (
    <section id="hero">
      <div className="hero-container" data-aos="fade-up" data-aos-delay="150">
       {/* This is the section handling the Hero image. It's connected on the CSS */}
      </div>
    </section>
  );
};

export default Hero;
